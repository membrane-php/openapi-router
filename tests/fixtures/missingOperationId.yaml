---
openapi: 3.0.0
info:
  title: Test API
  version: 1.0.0
servers:
  - url: http://test.com
paths:
  "/path":
    get:
      parameters:
        - name: id
          in: header
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: integer
